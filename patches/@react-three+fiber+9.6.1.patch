diff --git a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
index 97d8295..f264750 100644
--- a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
+++ b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
@@ -1013,7 +1013,46 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      // R3F still uses THREE.Clock; Three r183+ logs deprecation on construction. Shim matches Clock API.
+      clock: (() => {
+        const clock = {
+          autoStart: true,
+          startTime: 0,
+          oldTime: 0,
+          elapsedTime: 0,
+          running: false,
+          start() {
+            this.startTime = performance.now();
+            this.oldTime = this.startTime;
+            this.elapsedTime = 0;
+            this.running = true;
+          },
+          stop() {
+            this.getElapsedTime();
+            this.running = false;
+            this.autoStart = false;
+          },
+          getElapsedTime() {
+            this.getDelta();
+            return this.elapsedTime;
+          },
+          getDelta() {
+            let diff = 0;
+            if (this.autoStart && !this.running) {
+              this.start();
+              return 0;
+            }
+            if (this.running) {
+              const newTime = performance.now();
+              diff = (newTime - this.oldTime) / 1000;
+              this.oldTime = newTime;
+              this.elapsedTime += diff;
+            }
+            return diff;
+          }
+        };
+        return clock;
+      })(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
